#' @param source Data source identifier (e.g. `"abagen"`, `"beliveau"`).
#' @param desc Map descriptor key (e.g. `"genepc1"`, `"feobv"`).
#' @param space Coordinate space (e.g. `"fsaverage"`, `"MNI152"`, `"fsLR"`).
#' @param density Surface vertex density (e.g. `"10k"`, `"164k"`). Mutually
#'   exclusive with `resolution`.
#' @param resolution Volume voxel resolution (e.g. `"1mm"`, `"2mm"`). Mutually
#'   exclusive with `density`.
#' @param hemisphere Hemisphere (`"L"` or `"R"`).
